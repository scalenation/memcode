{
  "name": "@memcode/pro",
  "version": "1.0.0",
  "description": "MemCode Pro — proprietary LLM summarizer, semantic recall, and cloud sync (private package)",
  "license": "UNLICENSED",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@memcode/core": "workspace:*",
    "@memcode/cloud-client": "workspace:*",
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.10",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "publishConfig": {
    "registry": "https://registry.memcode.dev",
    "access": "restricted"
  }
}
