{
  "name": "@memcode/cloud-client",
  "version": "1.0.0",
  "description": "Optional encrypted cloud sync client for MemCode (Pro feature)",
  "keywords": [
    "memory",
    "sync",
    "cloud",
    "pro"
  ],
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@memcode/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.15.0"
  }
}
