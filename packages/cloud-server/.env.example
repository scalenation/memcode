# Required — database
DATABASE_URL=postgres://memcode:memcode@localhost:5432/memcode

# Required — JWT signing secret (generate with: openssl rand -hex 32)
JWT_SECRET=change_me_to_a_random_secret

# Required — Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Price ID for the $3.99/month Pro plan (create in Stripe Dashboard → Products)
STRIPE_PRICE_ID=price_...

# Optional
PORT=3000
HOST=0.0.0.0
APP_URL=https://memcode.dev
