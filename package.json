{
  "name": "memcode",
  "version": "0.1.0",
  "private": true,
  "description": "Project Memory Assistant \u2014 local-first memory for coding assistants",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "demo": "bash scripts/demo.sh",
    "release": "bash scripts/release.sh"
  },
  "engines": {
    "node": ">=22.15.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
